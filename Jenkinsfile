pipeline {
    agent {
        label 'my-agent'
    }
    stages {
        stage('Build') {
            steps {
            echo 'Build'
                // define build steps here
            }
        }
        stage('Test') {
            steps {
            echo 'Test'
                // define test steps here
            }
        }
        stage('Deploy') {
            steps {
             echo 'Deploy'
                // define deployment steps here
            }
        }
    }
}